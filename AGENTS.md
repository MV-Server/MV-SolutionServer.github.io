# MV-SolutionServer.github.io — 휴면. 되살리기 전에 읽는다

GitHub Pages 저장소로 보이는 것. 계열 지도에도 **설명 없음**으로 남아 있다.

**사람이 마지막으로 커밋한 날은 2022-11-30 이다.** 그 뒤로 코드가 멈춰 있다.

2026-08 에 찍힌 커밋들은 사람의 작업이 아니다. 저장소 문서화 자동화가 남긴
README 신원 카드와 시크릿 스캔 워크플로 배포다. **저장소 목록의 `pushedAt`
으로 이 저장소가 살아 있다고 판단하지 않는다.**

## 스택

- 확인되지 않음

## 지금 무엇이 이 자리를 맡고 있나

- 후속 저장소가 확인되지 않았다.

정확한 계보는 브레인(dev 소스)의 계열 지도에 있다 — `repos/singles`.

## 명령

**확인되지 않았다.** 4년 넘게 아무도 돌리지 않은 저장소라 `package.json` 이나
빌드 파일에 적힌 명령이 지금도 도는지 검증된 바 없다.

**여기에 명령을 추측해서 적지 않는다.** 되살리기로 결정한 사람이 실제로 돌려
보고 exit 0 을 확인한 뒤 이 절을 채운다. 틀린 명령이 적힌 완료 기준을 한 번
만나면 에이전트는 완료 기준 전체를 무시하기 시작한다.

## 완료 기준

이 저장소에 대한 작업은 **기본적으로 없다.** 요청받았다면 아래 순서다.

1. 아래 「되살리기 전에」를 먼저 확인하고, 되살릴 저장소가 맞는지 보고한다.
2. 그래도 손대게 되면 `gitleaks git --staged --no-banner` exit 0.
   CI(`.github/workflows/secret-scan.yml`)가 쓰는 것과 같은 도구다.
   **직접 grep 을 짜지 않는다.**

## 되살리기 전에

- **계열 지도를 먼저 읽는다.** 브레인 dev 소스의 `repos/singles` 에 이 저장소가
  단독 저장소 계열의 어디에 놓이는지 적혀 있다. 지금 살아 있는 저장소가
  무엇인지도 거기 있다.
- **인증 상태를 확인한다.** 같은 시기에 멈춘 WSS 계열은 인증 검사가 전부
  주석 처리된 채로 멈춰 있다. 휴면 저장소에서 반복되는 형태다.
- **공통 유틸을 고치기 전에 몇 벌이 있는지 센다.** 사내 공유 패키지가 없어
  새 프로젝트가 앞 프로젝트를 복사해 시작했다. `UtilLib.py` 는 29벌,
  `GeometryUtil.kt` 는 5벌이고 인자 순서까지 다르다.
- **좌표계는 주석을 믿지 않는다.** `lon_0` 값을 본다 — 125=서부, 127=중부,
  129=동부, 131=동해. 코드에 있는 `EPSG:5286` 은 5186 의 오타가 복제된 것이다.

## 건드리지 않는 것

- **커밋된 자격증명을 임의로 지우지 않는다.** 쓰이고 있는 키를 먼저 지우면
  서비스가 멈춘다. 교체할 키를 준비하고 전환한 뒤 정리한다. 목록은
  `mv-brain-infra` 의 `repos-docs/SECRET-FINDINGS.md` 에 있다.
- **저장소를 아카이브하거나 삭제하지 않는다.** 별도 판단이다.

## 이 저장소의 함정

- **이름이 조직명(`MV-SolutionServer`)을 담고 있다.** 조직 이름은 지금
  `MV-Server` 라 옛 이름의 흔적이다. 검색할 때 둘 다 봐야 한다.

## 막혔을 때

- 되살릴 저장소가 맞는지 판단이 안 서면 멈추고 보고한다. 여기서 고친 것이
  지금 쓰이는 저장소에 반영되지 않는 것이 이 문서가 막으려는 상황이다.
- 절대 하지 않는다: force push, 히스토리 재작성, 자격증명을 코드에 넣기,
  검증 없이 명령을 이 문서에 적기.
